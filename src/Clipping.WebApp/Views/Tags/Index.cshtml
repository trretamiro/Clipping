@model IEnumerable<Clipping.WebApp.Models.TagViewModel>

@{
    ViewData["Title"] = "Tags";
}

<h1>Lista de Tags</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Criar Tag</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Descricao)
                </td>
                <td>
                    <botao tipo-btn="Detalhes" route-id="@item.Id"></botao>
                    <botao tipo-btn="Editar" route-id="@item.Id"></botao>
                    <botao tipo-btn="Excluir" route-id="@item.Id"></botao>
                </td>
            </tr>
        }
    </tbody>
</table>
